const apiUrl: string = 'https://localhost:5001/api';

export const authUrl: string = `${apiUrl}/authentication`;
export const userUrl: string = `${apiUrl}/user`;
export const postUrl: string = `${apiUrl}/post`;